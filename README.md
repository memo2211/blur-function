# blur-function
MATLAB function that applies a blur effect to a grayscale image. Each pixel is replaced by the average of its neighbors in a square window of size 2w+1. Edges are handled using valid pixels only. The output is a blurred image of type uint8.
